{
  "id": "f0e1d2c3-iam-env-0001",
  "name": "IAM — Local",
  "values": [
    { "key": "base_url", "value": "http://localhost:8080", "enabled": true },
    { "key": "admin_email", "value": "admin@iam.local", "enabled": true },
    { "key": "admin_password", "value": "admin12345", "enabled": true },
    { "key": "user_email", "value": "alice@iam.local", "enabled": true },
    { "key": "user_password", "value": "alicepass123", "enabled": true }
  ],
  "_comment": "Tokens (access_token, refresh_token, token_type, admin_access_token, user_id) are intentionally NOT defined here. They live as COLLECTION variables and are written by post-response scripts. Keeping them out of the environment avoids env-shadows-collection issues in Postman, and the scripts translate cleanly to bru.setVar when imported into Bruno.",
  "_postman_variable_scope": "environment"
}
