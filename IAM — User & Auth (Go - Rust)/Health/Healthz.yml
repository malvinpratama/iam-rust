info:
  name: Healthz
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/healthz"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Liveness check. Public.
